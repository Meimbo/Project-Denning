import 'package:flutter/material.dart';

class TutorialItem extends StatelessWidget {
  const TutorialItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class TutorialViewPage extends StatelessWidget {
  const TutorialViewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
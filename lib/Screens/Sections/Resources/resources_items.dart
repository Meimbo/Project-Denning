import 'package:flutter/material.dart';

class ResourcesItems extends StatelessWidget {
  const ResourcesItems({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class ResourcesDetailsPage extends StatelessWidget {
  const ResourcesDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}